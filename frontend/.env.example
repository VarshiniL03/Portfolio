# In local dev, leave this blank — Vite proxies /api to localhost:8000 (see vite.config.js).
# In production (Vercel), set this to your deployed FastAPI backend URL, e.g.:
# VITE_API_BASE_URL=https://your-backend.onrender.com/api/v1
VITE_API_BASE_URL=
